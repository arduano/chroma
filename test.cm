// type A = {
//     a: {
//         a: {
//             a: C
//         },
//         b: string
//     }
// };

// type C = D;
// type D = A;

// type B = {
//     a: {
//         a: B,
//     },
// };

type Foo = { a: string, rec: Foo || string } || number || Bar;
type Bar = { a: "string", rec: Bar };

type A = Bar;

type B = Foo;

// type A = (1 || 2 || 3 || 4 || 5) % 2;
// type B = 0 || 1;
